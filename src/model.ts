export interface UserIds {
  userIds: number[];
}
export interface UserPresence {
  userPresenceType: number;
  lastLocation: string;
  placeId: any;
  rootPlaceId: any;
  gameId: any;
  universeId: any;
  userId: number;
  lastOnline: string;
}
